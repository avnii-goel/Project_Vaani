import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   /* config options here */
// };
// next.config.ts
const nextConfig = {
  experimental: {
    // fallback if fonts unreachable
  }
}
export default nextConfig;
